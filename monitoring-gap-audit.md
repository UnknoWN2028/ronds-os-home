# 智慧视频监控页面差异审查

## 2026-07-17 完善结果

本轮已完成此前列出的 P1-P3 前端缺口：设备选择、分页和轮播已驱动画面；视频墙比例已扩大；所有有效画面均提供播放、静音、全屏和更多操作；设备树支持展开/收起、父子级联、部分选中和本地记忆；报警筛选、状态、统计和详情处理已联动；共享工作区增加了可关闭的多页签。浏览器自动交互检查 12 项全部通过，证据见 `monitoring-interaction-qa-result.html`。

完整 PRD 的剩余前端模块也已补齐：右侧栏支持收起/展开并驱动视频墙自适应；点击视频可切换到对应设备的云台控制和视频回放；云台包含方向、调焦、聚焦、光圈、对讲、补光灯、雨刷、录制和重启状态；视频回放包含上下位机、排序、录制方式筛选、列配置和分页；最近报警支持自动轮播、悬停暂停、滚轮翻行以及多附件切换。证据见 `monitoring-full-prd-qa-result.html`，8 项完整 PRD 交互检查全部通过。

随后完成 1440px 工作站优化：中等桌面宽度下右侧栏默认收起，展开时改为覆盖式面板，不再压缩视频墙；右栏状态会被浏览器记忆；设备统计和报警卡片使用紧凑布局；轮播、分页、分屏工具栏保持完整可见。证据见 `monitoring-responsive-qa-result.html`，8 项响应式检查全部通过。

根据最终反馈，右栏改为推挤视频的非遮挡布局：在 1500px 及以下展开右栏时，左侧设备树始终保持 220px 完整宽度，右侧功能栏以 300px 独立网格列进入，只压缩中间四宫格视频墙。窄状态下视频工具栏会隐藏冗余单位文字并缩小按钮间距，轮播、分页和分屏仍完整可用。证据见 `monitoring-compress-qa-result.html`，左栏固定、视频压缩、无重叠、右栏占位、工具栏完整和四路画面可见共 6 项检查全部通过。

审查日期：2026-07-17  
参照页面：`http://192.168.2.39/PQSP7L/?id=45agsn&p=智慧视频监控&g=1`  
本地页面：`http://127.0.0.1:4173/video-monitoring`

## 审查范围

本次只核对“智慧视频监控”单页：设备树、四宫格视频墙、轮播工具栏、今日报警统计、最近报警列表，以及与主系统外壳的衔接。参照地址是 Axure Preview 页面，因此截图可确认可见结构与状态，不能证明原型中每个控件的完整行为。

## 截图步骤

1. 参照页面（健康度：可作为结构与视觉依据）

   ![参照页面](audit-source-monitoring.png)

2. 本地复刻页面（健康度：核心结构完整，但关键业务联动仍是演示状态）

   ![本地复刻页面](audit-local-monitoring.png)

## 已覆盖内容

- 三栏结构完整：设备树、四宫格视频墙、报警统计和报警列表均已存在。
- 搜索、设备勾选、轮播按钮、分页、画面布局切换、报警筛选和报警卡片选中状态已有基础交互。
- 主页面的顶部栏、侧边栏、面包屑和浅色设计语言已统一；这是需求指定的改造，不应视为遗漏。
- 摄像机编号、时间、加载状态、缩略图、报警等级、位置和时间等核心信息层级已覆盖。

## 主要遗漏与风险

### P1：视频墙占比不足

参照页面明显以视频为主，中心区域约占业务画布的三分之二；本地页面的设备栏和报警栏偏宽，四路视频被压缩。共享侧边栏可以保留，但业务区建议收窄设备栏与报警栏，把新增空间优先给视频墙。

### P1：设备、分页和轮播尚未驱动画面

当前设备勾选只改变勾选状态；分页只改变页码；“开始/暂停”只改变按钮文字，没有切换摄像机组或按间隔轮播。参照页面表达的是设备选择与实时画面的直接关系，这是当前最大的功能缺口。

### P1：实时画面的单路控制不完整

参照页面四个画面都有播放/暂停、进度或时间、音量、全屏，并显示更多操作；本地页面的控制条只出现在加载占位画面，已有图片的画面没有控制条，也缺少更多操作入口和悬浮快捷工具。

### P2：设备树层级行为不完整

参照页面有站点、廊道和摄像机的展开/收起层级，并同时存在选中与未选中设备。本地页面使用缩进模拟层级，但没有节点展开/收起、父子联动和部分选中状态。

### P2：报警状态维度不足

参照页面同时展示“待处理”“已关闭”等状态，并有不同等级与不同报警类型；本地四张卡片全部是待处理，筛选中的“全部”反而只显示更少记录，语义与预期不符。报警卡片也没有详情、定位或处理动作。

### P2：统计数据与参照状态不一致

参照页面为 50% 完成率，并展示今日新增 10、今日处理 10、今日转缺陷 8、待处理 10；本地为 100%、0、0、0、61。若是演示数据可以不同，但四项数据需要彼此自洽，并应与下方报警列表联动。

### P3：页签模型被简化

参照页面有多个可关闭业务页签，本地统一成单个面包屑标签。若主系统只需要侧边栏导航，这一差异可以保留；若需要同时打开多个业务页面，则还缺少页签历史、切换和关闭能力。

## 可访问性风险

- 设备行目前以 `aria-selected` 表达勾选，更接近树形复选框的语义应是 `aria-checked`，并补充展开节点的 `aria-expanded`。
- 视频工具栏中的全屏图标不是按钮，键盘用户无法聚焦或触发。
- 颜色承担了设备在线/离线和报警等级的主要识别责任，需要保留文字或图形双重提示。
- 截图无法确认完整键盘顺序、焦点可见性、读屏播报以及动态轮播是否可暂停，这些仍需浏览器交互测试。

## 建议实施顺序

1. 先把设备选择、分页和轮播与四路画面数据真正联动。
2. 补齐每路视频的播放、静音、全屏和更多操作，并统一加载、离线、失败、播放中状态。
3. 调整三栏比例，优先扩大视频墙；共享侧边栏与浅色主题保持不变。
4. 完善设备树展开/收起与父子选择逻辑。
5. 增加多状态报警数据、筛选、详情与处理入口，再让统计卡与列表同步。

## 结论

核心信息架构没有漏，但目前更接近“可交互的静态展示”。若目标是达到参照原型的业务完整度，优先补齐视频数据联动、单路控制和视频墙空间，其次是设备树与报警闭环。
